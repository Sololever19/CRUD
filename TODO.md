# ClientHub MySQL Backend Migration - TODO

- [ ] Confirm backend stack: Node.js/Express (done)
- [ ] Add backend files (server folder)
- [ ] Create MySQL schema (customers table)
- [ ] Implement REST API endpoints (auth + customers CRUD)
- [ ] Update frontend `js/db.js` to call backend APIs
- [ ] Update frontend `js/auth.js` to login via backend and store JWT
- [ ] Update frontend validation/duplicate checks to rely on backend unique constraints
- [ ] Install backend dependencies (`npm install` inside server)
- [ ] Create `.env` from `.env.example`
- [ ] Run backend (`node index.js`)
- [ ] Test: login, add/edit/delete customers, search, reports

